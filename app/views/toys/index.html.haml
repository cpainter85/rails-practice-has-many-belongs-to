.page-header
  %h1 Toys!

%table.table
  %thead
    %tr
      %th Toy Name
      %th Cat
  %tbody
    -@toys.each do |toy|
      %tr
        %td=link_to toy.name, toy_path(toy)
        %td
          =link_to toy.cat.name, cat_path(toy.cat)
          -# %em Replace this with a link to the cat who owns this toy
